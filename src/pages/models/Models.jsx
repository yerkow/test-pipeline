import { ModelsList } from "@/widgets";
import { Container } from "@mui/material";
import React from "react";

export const Models = () => {
  return (
    <Container>
      <ModelsList />
    </Container>
  );
};
